{% extends 'base.html.twig' %}

{% block title %}Candidat{% endblock %}

{% block body %}
    <h1>Candidat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidat.id }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ candidat.cv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_candidat_index') }}">back to list</a>

    <a href="{{ path('app_admin_candidat_edit', {'id': candidat.id}) }}">edit</a>

    {{ include('admin_candidat/_delete_form.html.twig') }}
{% endblock %}
